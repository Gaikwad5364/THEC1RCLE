import PlaceholderPage from "../../../components/host-layout/PlaceholderPage";

export default function CreateEventPage() {
    return <PlaceholderPage title="Create New Event" description="Launch a new event. Select 'Club Partner' to request a slot or 'Independent' for your own venue." />;
}
