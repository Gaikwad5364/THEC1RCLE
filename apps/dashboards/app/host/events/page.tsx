import PlaceholderPage from "../../../components/host-layout/PlaceholderPage";

export default function EventsPage() {
    return <PlaceholderPage title="Events Calendar & Pipeline" description="Manage your event schedule, track status from draft to live, and request slots from partner clubs." />;
}
